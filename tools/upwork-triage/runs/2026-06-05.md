# 2026-06-05 — triage rows 19-77 → scored 19-54 → final triage 19-51

## Context

- Pipeline migration since last triage (2026-05-08): apex-pvc `score_rows.py` is now authoritative for scoring; UpWork_Log column layout shifted (T = Priority Flag, W = Combined Score). Triton's commit `eb1d1c2` (2026-06-04) updated `tools/upwork-triage/upwork_triage.py` `write` to target col T — script is now safe.
- Email ingestion (07A) confirmed: pulls only **unread** `@_UpWork` Gmail, marks Read after sheet append.
- Parallel v2 pipeline exists (`upwork-v2-pipeline` skill, sheet `1zfCg4cjugpUj2_h4t6lGcUtt_x5ytCxjGkxvaN_ioRs`) — this run targets the legacy `UpWork_Log` sheet only.

## What happened

1. **First attempt (wrong)** — invoked legacy `upwork_triage.py write` (in this session, BEFORE rebasing onto Triton's fix) for rows 19-77 with `✅ Apply` / `skip` decisions. Strings went into col W, which is now Combined Score. Trashed the score column for those 59 rows.
2. Richard deleted the skip rows manually, leaving rows 19-54 (the applies). Asked to re-score.
3. Ran `python3 pvc2/score_rows.py --rows 19-54` — overwrote W with proper numeric scores and populated T (Priority Flag) since T was empty on all 36 rows.
4. Richard deleted 3 more rows (original 29, 34, 44 — the holds + the W=10 skip), compacting to rows 19-51 (33 rows).
5. Human triage pass on rows 19-51 — no overrides needed. Scorer's T assignments all pass standing skip criteria.

## Final triage outcome (rows 19-51, 33 rows)

| Combined Score | Priority Flag | Count |
|---|---|---|
| 18 | 🔥 Hot | 8 |
| 17 | 🔥 Hot | 14 |
| 16 | 🔥 Hot / ✅ Apply | 4 |
| 15 | 🔥 Hot / ✅ Apply | 3 |
| 14 | ✅ Apply | 3 |
| 12 | ✅ Apply | 1 |

25 🔥 Hot, 8 ✅ Apply, 0 ⏸ Hold, 0 ❌ Skip.

32 rows at W ≥ 14 (PVC2-eligible per score_rows.py threshold). Row 49 (legal referrals, W=12) is sub-threshold for PVC2 but still ✅ Apply for cover-letter-only.

## Lessons baked into the agent definition

- Score-then-triage is the canonical workflow.
- Standing skip criteria still override scorer output (skip wins).
- Cross-reference added pointing to `upwork-v2-pipeline` for the new v2 sheet/pipeline.
- Read-side of legacy `upwork_triage.py` remains usable; writes are now correctly targeted at col T after Triton's fix.
